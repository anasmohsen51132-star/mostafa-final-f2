// src/app/(owner)/loading.tsx
// NEXT-002 FIX: see (student)/loading.tsx for context.
import { FullScreenSpinner } from "@/components/ui/FullScreenSpinner";

export default function OwnerLoading() {
  return <FullScreenSpinner />;
}
